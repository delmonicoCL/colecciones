{
    "configurations": [
    {
        "name": "Launch Edge",
        "request": "launch",
        "type": "msedge",
        "url": "http://localhost:80/col/colecciones",
        "webRoot": "${workspaceFolder}"
    }
    ]
}